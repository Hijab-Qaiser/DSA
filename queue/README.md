# Queue Implementations

This folder contains my C++ implementations of:

- Linear Queue
- Circular Queue

The linear queue does not reuse empty positions created after dequeue operations.

The circular queue reuses available positions by allowing the pointers to return to the beginning of the array.
